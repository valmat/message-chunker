# MessageChunker Monorepo

Structure:
- docs/ — design documents and specifications
- packages/message-chunker — reusable Node.js module
- packages/message-chunker/src — source code
- packages/message-chunker/test — tests (Node.js test runner)

Getting started
1) Install Node.js >= 18
2) Run tests: `npm test`

